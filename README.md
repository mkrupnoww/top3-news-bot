# TOP 3 NEWS — Telegram-бот киноновостей

Telegram-бот для автоматизированного поиска, анализа, подготовки и публикации трёх главных киноновостей дня.

Основные интеллектуальные этапы выполняет OpenAI-агент, работающий через API OpenAI:

* поиск актуальной информации в настроенных источниках;
* извлечение и проверка фактов;
* очистка, нормализация и дедупликация материалов;
* анализ каждой новости по формуле из 16 параметров с весами;
* ранжирование кандидатов и выбор TOP-3;
* генерация текста публикации;
* форматирование текста для Telegram;
* генерация единой PNG-иллюстрации для трёх новостей;
* подготовка публикации к отправке по расписанию.

Фактическую отправку сообщения в Telegram-канал выполняет приложение через Telegram Bot API.

На этапе MVP публикация возможна только после подтверждения человеком. Отдельный проверочный прогон OpenAI для сгенерированного поста уже реализован: после основной генерации выполняется отдельный запрос к той же модели с проверочным промптом, а результат self-review сохраняется вместе с данными генерации. На текущем этапе этот прогон не заменяет ручное подтверждение. После накопления достаточной статистики его результаты могут использоваться как один из механизмов безопасной автоматической публикации по расписанию.

## Целевой сценарий работы

```text
Запуск ежедневного задания по расписанию
        ↓
OpenAI-агент через API OpenAI
        ↓
Поиск актуальных киноновостей
        ↓
Получение исходных материалов и фактов
        ↓
Очистка и нормализация
        ↓
Дедупликация
        ↓
Анализ по 16 параметрам с весами
        ↓
Расчёт итогового рейтинга
        ↓
Фильтр качества и разнообразия
        ↓
Выбор TOP-3
        ↓
Генерация текста публикации
        ↓
Форматирование для Telegram
        ↓
Генерация общей PNG-иллюстрации
        ↓
Отдельный проверочный прогон OpenAI (self-review)
        ↓
Подтверждение человеком
        ↓
Публикация через Telegram Bot API
```

## Сценарий MVP

В первой версии сохраняется обязательный контроль со стороны человека:

```text
Источники киноновостей
        ↓
Сбор и обработка материалов
        ↓
Оценка и выбор TOP-3
        ↓
Генерация текста
        ↓
Генерация общей PNG-иллюстрации
        ↓
Предпросмотр в Telegram
        ↓
Подтверждение человеком
        ↓
Публикация в Telegram-канале
```

Без явного подтверждения ответственного пользователя пост на этапе MVP не публикуется.

## Оценка и выбор TOP-3

Каждая новость анализируется OpenAI-агентом по 16 содержательным параметрам. Для каждого параметра задаётся собственный вес.

Версии формулы, параметров, модели и промпта должны фиксироваться, чтобы каждый запуск ранжирования можно было воспроизвести и проверить.

В текущей модели используется итоговый индивидуальный балл новости:

```text
B_i = 0.20 × F_i
    + 0.30 × M_i
    + 0.20 × R_i
    + 0.15 × (H_i × Q_i)
```

Где:

* `F` — свежесть и актуальность новости;
* `M` — масштаб события и потенциальный охват;
* `R` — общественный резонанс и вовлечённость аудитории;
* `H` — скандальность, необычность или другой цепляющий элемент;
* `Q` — подтверждённость информации и качество источников.

Для отбора используются только новости из установленного временного окна, как правило за последние 24 часа.

После индивидуального ранжирования применяется фильтр разнообразия: итоговая тройка не должна состоять из трёх однотипных новостей, например только из сообщений о кассовых сборах.

Для каждой оценки сохраняются:

* значения компонентов формулы;
* итоговый балл;
* позиция в рейтинге;
* признак допуска к отбору;
* причина исключения;
* текстовое объяснение оценки;
* дополнительные структурированные данные анализа.

## Роль OpenAI-агента

OpenAI-агент управляет интеллектуальным сценарием подготовки ежедневной публикации.

Он должен:

1. Найти актуальные новости в разрешённых источниках.
2. Выделить исходные факты, даты, имена, названия и ссылки.
3. Сопоставить сообщения из разных источников.
4. Исключить дубли и слабоподтверждённые материалы.
5. Оценить новости по 16 параметрам с установленными весами.
6. Выбрать три наиболее значимые и при этом разнообразные темы.
7. Подготовить текст своими словами без копирования исходных публикаций.
8. Добавить проверяемые ссылки на первичные или официальные источники.
9. Отформатировать публикацию для Telegram.
10. Создать промпт и сгенерировать единую PNG-иллюстрацию.
11. Выполнить отдельный проверочный прогон OpenAI (self-review) для подготовленного поста.
12. Сохранить результат проверки и подготовить публикацию к ручному подтверждению.

Все значимые результаты работы агента должны сохраняться в PostgreSQL: найденные материалы, оценки, выбранные новости, версии промптов, сгенерированные тексты, изображения, результаты проверок и попытки публикации.

## Подтверждение публикации

На этапе MVP бот отправляет подготовленный пост ответственному пользователю и предоставляет действия:

* опубликовать;
* перегенерировать текст;
* перегенерировать изображение;
* заменить одну из новостей;
* отклонить публикацию.

Решение пользователя и все выполненные действия сохраняются в PostgreSQL.

## Статусы публикации

Предполагаемый жизненный цикл:

```text
draft
collected
ranked
generated
awaiting_review
approved
rejected
publishing
published
failed
```

## Проверочный прогон OpenAI

После основной генерации поста выполняется отдельный проверочный прогон OpenAI (self-review). Используется отдельный запрос к той же настроенной модели с самостоятельным проверочным промптом и отдельным результатом проверки.

Основной генерационный запрос и проверочный прогон разделены. Проверка выполняется после формирования текста публикации и предназначена для выявления проблем до передачи поста человеку на подтверждение.

Проверочный прогон оценивает:

* соответствие текста исходным материалам;
* наличие неподтверждённых утверждений;
* точность имён, дат, названий и числовых данных;
* корректность ссылок и источников;
* соответствие выбранных новостей правилам TOP-3;
* качество заголовков и текста;
* правильность Telegram-форматирования;
* пригодность публикации без ручного редактирования.

Результат self-review сохраняется вместе с данными генерации и используется как дополнительный контроль качества.

На текущем этапе отдельный проверочный прогон не может самостоятельно разрешить публикацию: итоговый пост получает статус `awaiting_review` и публикуется только после явного подтверждения человеком.

В дальнейшем, после накопления достаточной статистики и подтверждения надёжности проверки, результаты self-review могут использоваться как один из механизмов безопасного перехода к автоматической публикации.

## Публикация по расписанию

Приложение должно запускать ежедневный сценарий по установленному расписанию.

OpenAI-агент выполняет поиск, анализ, выбор новостей и генерацию материалов. После получения разрешения на публикацию приложение отправляет готовый текст и изображение в Telegram-канал через Telegram Bot API.

Для каждой попытки публикации сохраняются:

* запланированное время;
* фактическое время запуска;
* идентификатор подготовленного поста;
* идентификатор Telegram-сообщения;
* статус отправки;
* ответ Telegram API;
* код и описание ошибки;
* номер повторной попытки.

## Структура проекта

```text
top3-news-bot/
├── app/
│   ├── bot/            # Telegram-интерфейс и обработчики команд
│   ├── collectors/     # Получение новостей из источников
│   ├── db/             # Работа с PostgreSQL
│   ├── generation/     # Генерация текста и изображений
│   ├── publication/    # Подготовка и публикация постов
│   ├── ranking/        # Оценка и выбор TOP-3
│   └── review/         # Ручная и AI-проверка
├── config/
│   └── systemd/        # Unit-файл системного сервиса
├── data/
│   └── images/         # Временные и итоговые изображения
├── logs/               # Логи приложения
├── migrations/         # Миграции PostgreSQL
├── prompts/            # Версионируемые промпты AI-моделей
├── scripts/            # Служебные скрипты
├── tests/              # Автоматические тесты
├── .env.example
├── .python-version
├── pyproject.toml
└── uv.lock
```

## Среды проекта

Разработка выполняется на ноутбуке в Cursor:

```text
/home/mihail/d/films/movie_news/top3-news-bot
```

Рабочее приложение запускается на сервере Ubuntu 24.04.3 LTS:

```text
/opt/top3-news-bot
```

Схема доставки кода:

```text
Cursor
   ↓
локальный Git
   ↓
GitHub
   ↓
автоматический деплой
   ↓
сервер Ubuntu 24.04.3 LTS
```

Приложение запускается как системная служба `systemd`:

```text
top3-news-bot.service
```

## Технологический стек

```text
Python 3.12
uv
aiogram
asyncpg
PostgreSQL 16
API OpenAI
Telegram Bot API
systemd
Git
GitHub
```

Локальная виртуальная среда:

```text
.venv
```

## Конфигурация и секреты

Локальные и серверные параметры приложения хранятся в файле:

```text
.env
```

Файл `.env` исключён из Git и не должен попадать в публичный репозиторий.

В репозитории хранится только безопасный шаблон:

```text
.env.example
```

Токены, пароли, закрытые ключи и другие секреты запрещено сохранять в исходном коде, README, миграциях, логах и истории Git.

## План разработки

1. Зафиксировать структуру проекта.
2. Спроектировать модель данных PostgreSQL.
3. Создать Telegram-бота через BotFather.
4. Настроить авторизацию пользователей через PostgreSQL.
5. Настроить запуск Telegram-бота как службы `systemd`.
6. Опубликовать проект в GitHub, настроить автоматический деплой.
7. Подключить тестовый приватный чат или Telegram-канал.
8. Реализовать сохранение публикаций и их статусов.
9. Реализовать предпросмотр и ручное подтверждение.
10. Подключить поиск и сбор источников новостей.
11. Реализовать очистку, нормализацию и дедупликацию.
12. Зафиксировать 16 параметров оценки и их веса.
13. Реализовать оценку и выбор TOP-3.
14. Подключить OpenAI-агента через API OpenAI.
15. Подключить генерацию текста и Telegram-форматирование.
16. Подключить генерацию общей PNG-иллюстрации.
17. Реализовать публикацию в Telegram-канале по расписанию.
18. Реализовать отдельный проверочный прогон OpenAI (self-review).
20. Настроить мониторинг, уведомления и резервное копирование.